<UserControl x:Class="ArcadiaTracker.App.Views.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ArcadiaTracker.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Notifications" Style="{StaticResource HeaderTextStyle}"/>
                <Border x:Name="UnreadBadge"
                        Grid.Column="1"
                        Background="{StaticResource PrimaryBrush}"
                        CornerRadius="12" Padding="12,6"
                        Visibility="Collapsed">
                    <TextBlock x:Name="UnreadCountText" Text="0 unread"
                               Foreground="{StaticResource BackgroundBrush}"
                               FontWeight="Bold" FontSize="14"/>
                </Border>
            </Grid>
            <TextBlock Text="System notifications and alerts"
                       Style="{StaticResource SubheaderTextStyle}" Margin="0,0,0,24"/>

            <!-- Filter Buttons -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Severity Filters -->
                <UniformGrid Grid.Column="0" Columns="5" Margin="0,0,16,0">
                    <Button x:Name="FilterAllButton"
                            Content="All"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="FilterAll_Click"
                            Margin="0,0,4,0"/>
                    <Button x:Name="FilterInfoButton"
                            Content="Info"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="FilterInfo_Click"
                            Margin="4,0,4,0"/>
                    <Button x:Name="FilterSuccessButton"
                            Content="Success"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="FilterSuccess_Click"
                            Margin="4,0,4,0"/>
                    <Button x:Name="FilterWarningButton"
                            Content="Warning"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="FilterWarning_Click"
                            Margin="4,0,4,0"/>
                    <Button x:Name="FilterCriticalButton"
                            Content="Critical"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="FilterCritical_Click"
                            Margin="4,0,0,0"/>
                </UniformGrid>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Mark All Read"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="MarkAllRead_Click"
                            Margin="0,0,8,0"/>
                    <Button Content="Clear"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="Clear_Click"/>
                </StackPanel>
            </Grid>

            <!-- Notifications List -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <ItemsControl x:Name="NotificationsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,8" Padding="12,10" CornerRadius="8"
                                        Background="{Binding BackgroundBrush}"
                                        MouseLeftButtonDown="Notification_Click"
                                        Tag="{Binding Id}"
                                        Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Icon -->
                                        <Border Grid.Column="0"
                                                Background="{Binding IconBackgroundBrush}"
                                                Width="40" Height="40"
                                                CornerRadius="20"
                                                Margin="0,0,16,0"
                                                VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Icon}"
                                                       FontSize="20"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Title and Message -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontWeight="SemiBold"
                                                       FontSize="14"/>
                                            <TextBlock Text="{Binding Message}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontSize="12"
                                                       Margin="0,4,0,0"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>

                                        <!-- Timestamp and Status -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Top"
                                                    HorizontalAlignment="Right" Margin="16,0,0,0">
                                            <TextBlock Text="{Binding TimestampDisplay}"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       FontSize="11"
                                                       HorizontalAlignment="Right"/>
                                            <Border Background="{StaticResource PrimaryBrush}"
                                                    CornerRadius="6"
                                                    Width="12" Height="12"
                                                    Margin="0,4,0,0"
                                                    HorizontalAlignment="Right"
                                                    Visibility="{Binding IsUnread, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Empty state -->
                    <TextBlock x:Name="NoNotificationsText"
                               Text="No notifications"
                               Foreground="{StaticResource TextMutedBrush}"
                               Visibility="Collapsed"
                               HorizontalAlignment="Center"
                               Margin="0,16"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
