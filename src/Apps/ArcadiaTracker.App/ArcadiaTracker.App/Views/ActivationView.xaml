<UserControl x:Class="ArcadiaTracker.App.Views.ActivationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArcadiaTracker.App.Views"
             xmlns:vm="clr-namespace:ArcadiaTracker.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource SurfaceBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <TextBlock Text="Unlock Pro Features"
                       FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Get access to Save Editor, Advanced Analytics, and more."
                       Foreground="{StaticResource TextSecondaryBrush}"
                       TextWrapping="Wrap" TextAlignment="Center"
                       Margin="0,0,0,30"/>

            <Button Command="{Binding PurchaseCommand}"
                    Content="Purchase License (Web)"
                    Style="{StaticResource PillButtonStyle}"
                    Margin="0,0,0,20" Height="40"/>

            <Separator Background="{StaticResource SurfaceLightBrush}" Margin="0,20"/>

            <TextBlock Text="Already have a key?"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,0,5"/>

            <TextBox Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource SciFiTextBox}"
                     MaxLength="512"
                     Height="40" VerticalContentAlignment="Center"/>

            <Button Command="{Binding ActivateCommand}"
                    Content="Activate"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                    Margin="0,10,0,0" Height="35"/>

            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{Binding IsError, Converter={StaticResource BoolToColorConverter}, ConverterParameter='Red|Green'}"
                       TextWrapping="Wrap" Margin="0,10,0,0"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
